var weekDays = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"]; var stylesList = ["Chinese / Asian", "Mexican / TexMex", "Italian / Med", "American Comfort", "Diner Breakfast"]; var proteinsList = ["Chicken", "Beef", "Turkey", "Seafood", "Pork"]; var proteinCals = { "Chicken": 640, "Beef": 710, "Turkey": 610, "Seafood": 380, "Pork": 640 }; var currentClockDate = new Date(); var currentDayIndex = currentClockDate.getDay(); var distanceToMonday = currentDayIndex === 0 ? -6 : 1 - currentDayIndex; var mondayDate = new Date(currentClockDate.getTime() + distanceToMonday * 24 * 60 * 60 * 1000); var weekLabelString = mondayDate.toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }); var weekHTML = '🗓️ Schedule for the Week of ' + weekLabelString + '📅 Export Week to Google Calendar'; for (var d = 0; d < 7; d++) { var rOpt = '- Cuisine -'; for (var s = 0; s < 5; s++) rOpt += ''+stylesList[s]+''; var pOpt = '- Protein -'; for (var p = 0; p < 5; p++) pOpt += ''+proteinsList[p]+''; var loopDayDate = new Date(mondayDate.getTime() + d * 24 * 60 * 60 * 1000); var dateStampLabel = loopDayDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric' }); weekHTML += '' + weekDays[d] + ' (' + dateStampLabel + ')0 cal' + rOpt + '' + pOpt + '- Select Meal -'; } document.getElementById("weekContainer").innerHTML = weekHTML; if(!document.getElementById("weeklyTotalVal")) { var dashHTML = 'WEEKLY TOTAL TRACK0 CALDAILY AVERAGE TARGET0 CAL'; document.querySelector(".card").insertAdjacentHTML("beforebegin", dashHTML); } window.updateMealOptions = function(dayIndex) { var sBox = document.getElementById("style-m1-" + dayIndex); var pBox = document.getElementById("prot-m1-" + dayIndex); if (!sBox || !pBox) return; var sVal = sBox.value; var pVal = pBox.value; var mSelect = document.getElementById("meal-m1-" + dayIndex); if (!mSelect) return; mSelect.innerHTML = '- Select Meal -'; if (!sVal || !pVal) { syncAppEngine(); return; } var baseCal = proteinCals[pVal] || 600; var cleanStyle = sVal.split(" / "); var optionsHTML = '- Select Meal -'; var pMethods = window.prepMethods || ["Standard"]; var sStyles = window.servingStyles || ["Plate"]; for (var i = 0; i < 35; i++) { var calOffset = (i % 2 === 0) ? (i * 2) : -(i * 1.5); var computedCal = Math.round(baseCal + calOffset); var dishName = pMethods[i % pMethods.length] + " " + cleanStyle + " " + pVal + " " + sStyles[(i + dayIndex) % sStyles.length] + " (" + computedCal + " cal)"; optionsHTML += '' + dishName + ''; } mSelect.innerHTML = optionsHTML; mSelect.selectedIndex = 1; syncAppEngine(); }; window.exportFullWeekToGoogleCalendar = function() { alert("🚀 Google Calendar Link Synced!\n\nThis will now generate your custom daily schedule events. Click OK to open your Google Calendar dashboard overlay link with your logged food plans pre-written!"); for (var d = 0; d < 7; d++) { var mealBox = document.getElementById("meal-m1-" + d); var mealText = mealBox && mealBox.value ? mealBox.value.split("|") : "No food meal logged."; var targetDayDate = new Date(mondayDate.getTime() + d * 24 * 60 * 60 * 1000); var isoDateString = targetDayDate.toISOString().replace(/-|:|.\d\d\d/g, "").substring(0,8); var calendarTitle = encodeURIComponent("🥗 Diet Plan: " + weekDays[d]); var calendarDetails = encodeURIComponent("🍽️ Solid Food: " + mealText); var calendarDates = isoDateString + "T120000Z/" + isoDateString + "T130000Z"; var googleCalendarLink = "google.com" + calendarTitle + "&dates=" + calendarDates + "&details=" + calendarDetails + "&sf=true&output=xml"; window.open(googleCalendarLink, '_blank'); break; } }; window.syncAppEngine = function() { var rHTML = "", activeMap = {}, masterGroceries = {}, grandCals = 0; for (var d = 0; d < 7; d++) { var m1Box = document.getElementById("meal-m1-" + d); var m1Val = m1Box ? m1Box.value : ""; var activeToday = [m1Val]; var dayCals = 0; var inputLiq = document.getElementById("liquid-total-badge-" + d); var liqVal = 0; if (inputLiq) { var rawText = inputLiq.innerText || ""; liqVal = parseInt(rawText.replace(/[^0-9]/g, "")) || 0; } dayCals += liqVal; var pGroceries = window.proteinGroceries || {}; var sGroceries = window.styleGroceries || {}; for (var m = 0; m < activeToday.length; m++) { var raw = activeToday[m]; if (!raw) continue; var frags = raw.split("|"); if (frags.length < 4) continue; var style = frags, protein = frags, fullDishName = frags, mealCalories = parseInt(frags) || 0; dayCals += mealCalories; var pData = pGroceries[protein], sData = sGroceries[style]; if (!pData || !sData) continue; if (!activeMap[fullDishName]) { activeMap[fullDishName] = true; rHTML += '' + fullDishName + 'Ingredients: ' + pData.item + ', ' + sData.side + ', ' + sData.prod + ', ' + sData.sauce + '.Directions: ' + sData.steps + ''; } var listItems = [{ name: pData.item, aisle: pData.dept }, { name: sData.side, aisle: "🌾 Grains & Pantry Supply" }, { name: sData.prod, aisle: "🥦 Fresh Produce Aisle" }, { name: sData.sauce, aisle: "🥫 Sauces & Seasonings" }]; for (var g = 0; g < listItems.length; g++) { var line = listItems[g]; if (!masterGroceries[line.aisle]) masterGroceries[line.aisle] = {}; masterGroceries[line.aisle][line.name] = (masterGroceries[line.aisle][line.name] || 0) + 1; } } grandCals += dayCals; var badge = document.getElementById("day-badge-" + d); if(badge) badge.innerHTML = dayCals + " cal"; } var totVal = document.getElementById("weeklyTotalVal"); if(totVal) totVal.innerHTML = grandCals + " CAL"; var avgVal = document.getElementById("dailyAvgVal"); if(avgVal) avgVal.innerHTML = Math.round(grandCals / 7) + " CAL"; var recBox = document.getElementById("recipeBox"); if(recBox) recBox.innerHTML = rHTML || "Select options above to view cooking instructions."; var grocBox = document.getElementById("groceryBox"); if(grocBox) { var gHTML = ""; for (var dept in masterGroceries) { gHTML += '' + dept + ''; for (var row in masterGroceries[dept]) { gHTML += '(' + masterGroceries[dept][row] + 'x) ' + row + ''; } gHTML += ''; } grocBox.innerHTML = gHTML || "Select options above to compile checklists."; } }; setTimeout(function() { for (var d = 0; d < 7; d++) { var sBox = document.getElementById("style-m1-" + d); var pBox = document.getElementById("prot-m1-" + d); if(sBox && pBox) { sBox.value = ["American Comfort", "Mexican / TexMex", "Italian / Med", "Chinese / Asian", "Italian / Med", "American Comfort", "American Comfort"][d]; pBox.value = ["Beef", "Beef", "Pork", "Seafood", "Seafood", "Beef", "Chicken"][d]; updateMealOptions(d); } } }, 300);
